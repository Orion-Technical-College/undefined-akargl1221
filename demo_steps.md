# Transaction + concurrency demo

Document a **two-session** demo (for example two `psql` terminals or two clients).

## Isolation / locking notes

Explain which isolation level you used, what locks or conflicts you observed, and how **MVCC** affects what each session sees.

## Session A / Session B

Replace with your actual command transcripts and paste exports into `evidence/sessionA.txt` and `evidence/sessionB.txt`.
